name: miniconda
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - awscli
  - sagemaker-python-sdk
  - pip
  - pip:
      - --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host files.pythonhosted.org sagemaker-training